"""
Module 2 — Lesson 1: Variables & Data Types
Student: [ADRIAN A TULABUT]
Date: [9/25/26]

============================================
WHAT IS THIS TOPIC? (explain it like you're
teaching a friend who's never coded before)
============================================
[this topic is what we call reserved variables or special syntax, you may see this on general programming languages 
and they mostly use the same thing (eg float, boolean etc etc) 
first of all we have variable, this is basically giving a code a name, so you wont get confused when calling it, and it will be
more easier to familiarize your code, for example:

name = "adrian" 

in this case i used the variable and named it "name" and i declared a string onto that variable named "adrian"

now for the data type, its basically asigning a value to a variable, earlier i used double quotes on "adrian" and this means i 
used the string data type, now lets use int, which is short for integer, this data type lets us use numerical numbers (eg '1' '6')
notice how i used single colons between the numbers, (string uses double colons "adrian" and integers use single colons '67')

for the float variable its basically the same as the string but this time it has decimal points (67.67) use this when working with 
decimal points (for example creating a calculator, where decimals are important)

next is boolean, this is a special one because unlike the others, this one lets us use only 2 choices, (true and false) 
we normally use this when working with system who only have either on or off, or true or false
for example (lights_on = false) (adrian_pogi = true)

we also have more like data types like char, which lets us store a single text, or a single string
for example (char initial 'A')

]


============================================
KEY VOCABULARY
============================================
- variable:
- data type:
- int:
- float:
- string:
- boolean:
(add more as needed)


============================================
MY OWN EXAMPLE(S)
============================================
Write at least one working example below that you
came up with yourself — not copied from class.
"""

name = "Adrian"
age = 12
initial = 'A'
handsome = True 
height = 6.2


"""
============================================
A MISTAKE I MADE (or one I want to avoid)
============================================
[what's something confusing or easy to get wrong
about this topic?

declaring datatypes and variables are really easy as long as you remember the syntaxes of them, for example when using string
remember to use double quotes and when using char, us single ones
]


============================================
HOW THIS CONNECTS TO SOMETHING ELSE
============================================
[
variables and datatypes are use everywhere, and you cant code without them, this is basically how coding works, naming a variable, 
giving it a datatype, and giving it a value, for example creating a calculator,
or for more complex projects like object oriented programming
which on that case, you can use a variable for naming the function
and calling the variable later on and it runs the function
] 
"""
